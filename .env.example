# =============================================================================
# PBXScribe Deployment Configuration
# Copy to .env and fill in the values — never commit .env.
# =============================================================================

# Environment: dev | staging | prod
ENVIRONMENT=dev

# AWS Configuration
AWS_REGION=us-east-2
AWS_PROFILE=default

# Application Secrets
# JWT_SECRET: min 32 characters — used to sign/verify JWT tokens
# Generate one with: openssl rand -base64 48
JWT_SECRET=

# MIGRATION_SECRET: min 16 characters — protects the POST /migrate endpoint
# Generate one with: openssl rand -base64 24
MIGRATION_SECRET=

# =============================================================================
# CI/CD Configuration (used by setup-cicd.sh)
# =============================================================================

# GitHub repository — org/username and repo name (without org prefix)
GITHUB_ORG=digicom-dts
GITHUB_REPO=pbxscribe

# Branch that triggers automated deployments
DEPLOY_BRANCH=main
